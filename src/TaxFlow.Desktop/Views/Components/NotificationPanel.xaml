<UserControl x:Class="TaxFlow.Desktop.Views.Components.NotificationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="350">
    <Border Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
            BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
            BorderThickness="1"
            CornerRadius="5"
            Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{DynamicResource MahApps.Brushes.Gray9}"
                    Padding="15,10"
                    CornerRadius="5,5,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" FlowDirection="RightToLeft">
                        <TextBlock Text="🔔" FontSize="18" Margin="0,0,5,0"/>
                        <TextBlock Text="الإشعارات"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="مسح الكل"
                            FontSize="10"
                            Padding="8,4"
                            VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Notifications List -->
            <ScrollViewer Grid.Row="1"
                         VerticalScrollBarVisibility="Auto"
                         Padding="5">
                <ItemsControl>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource MahApps.Brushes.Gray10}"
                                    BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                    BorderThickness="0,0,0,1"
                                    Padding="12"
                                    Margin="0,0,0,2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Icon and Title -->
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" FlowDirection="RightToLeft">
                                        <TextBlock Text="ℹ️" FontSize="16" Margin="0,0,5,0"/>
                                        <TextBlock Text="عنوان الإشعار"
                                                   FontWeight="Bold"
                                                   FontSize="13"/>
                                    </StackPanel>

                                    <!-- Message -->
                                    <TextBlock Grid.Row="1"
                                               Text="رسالة الإشعار التفصيلية..."
                                               TextWrapping="Wrap"
                                               FontSize="12"
                                               Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                               Margin="0,5,0,5"
                                               FlowDirection="RightToLeft"/>

                                    <!-- Time -->
                                    <TextBlock Grid.Row="2"
                                               Text="منذ 5 دقائق"
                                               FontSize="10"
                                               Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                               FlowDirection="RightToLeft"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                    <!-- Sample Notifications -->
                    <Border Background="{DynamicResource MahApps.Brushes.Gray10}"
                            BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                            BorderThickness="0,0,0,1"
                            Padding="12"
                            Margin="0,0,0,2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" FlowDirection="RightToLeft">
                                <TextBlock Text="✅" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="تم إرسال الفاتورة بنجاح"
                                           FontWeight="Bold"
                                           FontSize="13"/>
                            </StackPanel>

                            <TextBlock Grid.Row="1"
                                       Text="تم إرسال الفاتورة INV-2024-001 إلى مصلحة الضرائب بنجاح"
                                       TextWrapping="Wrap"
                                       FontSize="12"
                                       Foreground="{DynamicResource MahApps.Brushes.Gray2}"
                                       Margin="0,5,0,5"
                                       FlowDirection="RightToLeft"/>

                            <TextBlock Grid.Row="2"
                                       Text="منذ دقيقتين"
                                       FontSize="10"
                                       Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                       FlowDirection="RightToLeft"/>
                        </Grid>
                    </Border>

                    <Border Background="#FFF3E0"
                            BorderBrush="#FFB74D"
                            BorderThickness="0,0,0,1"
                            Padding="12"
                            Margin="0,0,0,2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" FlowDirection="RightToLeft">
                                <TextBlock Text="⚠️" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="تحذير: شهادة قريبة من الانتهاء"
                                           FontWeight="Bold"
                                           FontSize="13"/>
                            </StackPanel>

                            <TextBlock Grid.Row="1"
                                       Text="الشهادة الرقمية الخاصة بك ستنتهي خلال 15 يوم"
                                       TextWrapping="Wrap"
                                       FontSize="12"
                                       Foreground="#E65100"
                                       Margin="0,5,0,5"
                                       FlowDirection="RightToLeft"/>

                            <TextBlock Grid.Row="2"
                                       Text="منذ ساعة"
                                       FontSize="10"
                                       Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                       FlowDirection="RightToLeft"/>
                        </Grid>
                    </Border>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2"
                    Background="{DynamicResource MahApps.Brushes.Gray9}"
                    Padding="10"
                    CornerRadius="0,0,5,5">
                <TextBlock Text="عرض جميع الإشعارات ←"
                           FontSize="12"
                           Foreground="{DynamicResource MahApps.Brushes.Accent}"
                           HorizontalAlignment="Center"
                           Cursor="Hand"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
