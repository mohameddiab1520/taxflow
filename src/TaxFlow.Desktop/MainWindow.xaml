<mah:MetroWindow x:Class="TaxFlow.Desktop.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:vm="clr-namespace:TaxFlow.Desktop.ViewModels"
                 Title="{Binding Title}"
                 Width="1400"
                 Height="900"
                 WindowStartupLocation="CenterScreen"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                 ResizeMode="CanResizeWithGrip"
                 ShowIconOnTitleBar="True">

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <!-- Language Switcher -->
            <Button Content="EN"
                    Command="{Binding SwitchLanguageCommand}"
                    CommandParameter="en"
                    ToolTip="Switch to English" />
            <Button Content="عربي"
                    Command="{Binding SwitchLanguageCommand}"
                    CommandParameter="ar"
                    ToolTip="التبديل إلى العربية" />

            <!-- Theme Toggle -->
            <Button Command="{Binding ToggleThemeCommand}"
                    ToolTip="Toggle Light/Dark Theme">
                <iconPacks:PackIconMaterial Kind="ThemeLightDark" />
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Sidebar -->
        <Border Grid.Column="0"
                Background="{DynamicResource MahApps.Brushes.Accent4}"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20,0,0">
                <!-- Logo/Title -->
                <TextBlock Text="TaxFlow"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,40"/>

                <!-- Navigation Buttons -->
                <Button Content="Dashboard"
                        Command="{Binding NavigateToDashboardCommand}"
                        Style="{StaticResource MahApps.Styles.Button.MetroSquare.Accent}"
                        Height="50"
                        Margin="10,5">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="ViewDashboard"
                                                            Width="20"
                                                            Height="20"
                                                            Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button Content="Invoices (B2B)"
                        Command="{Binding NavigateToInvoicesCommand}"
                        Style="{StaticResource MahApps.Styles.Button.MetroSquare.Accent}"
                        Height="50"
                        Margin="10,5">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="FileDocument"
                                                            Width="20"
                                                            Height="20"
                                                            Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button Content="Receipts (B2C)"
                        Command="{Binding NavigateToReceiptsCommand}"
                        Style="{StaticResource MahApps.Styles.Button.MetroSquare.Accent}"
                        Height="50"
                        Margin="10,5">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Receipt"
                                                            Width="20"
                                                            Height="20"
                                                            Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button Content="Customers"
                        Command="{Binding NavigateToCustomersCommand}"
                        Style="{StaticResource MahApps.Styles.Button.MetroSquare.Accent}"
                        Height="50"
                        Margin="10,5">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="AccountMultiple"
                                                            Width="20"
                                                            Height="20"
                                                            Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button Content="Settings"
                        Command="{Binding NavigateToSettingsCommand}"
                        Style="{StaticResource MahApps.Styles.Button.MetroSquare.Accent}"
                        Height="50"
                        Margin="10,5">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Settings"
                                                            Width="20"
                                                            Height="20"
                                                            Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <ContentControl Content="{Binding CurrentViewModel}"
                            Margin="20"/>

            <!-- Loading Overlay -->
            <Grid Background="#80000000"
                  Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <mah:ProgressRing IsActive="{Binding IsBusy}"
                                      Width="60"
                                      Height="60"/>
                    <TextBlock Text="{Binding BusyMessage}"
                               Foreground="White"
                               FontSize="16"
                               Margin="0,20,0,0"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</mah:MetroWindow>
